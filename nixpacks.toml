[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = ["python3 -m venv .venv", ".venv/bin/pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = ".venv/bin/python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
